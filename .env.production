# 운영(Vercel) 빌드 시 사용되는 기본값.
# Phase 3 배포 후 DuckDNS 서브도메인이 확정되면 이 값을 교체하거나,
# Vercel Dashboard → Settings → Environment Variables 에서 VITE_API_BASE 를 설정해 덮어쓴다.
VITE_API_BASE=https://latale-api.duckdns.org
